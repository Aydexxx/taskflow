{
  "name": "@taskflow/shared",
  "version": "0.1.0",
  "description": "Shared TypeScript types: the single source of truth for the TaskFlow API + Socket.IO contract.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "prepare": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
